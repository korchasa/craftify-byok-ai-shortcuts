---
description:
globs:
alwaysApply: true
---
## TDD Flow
1.	Red:
- Write a minimal, isolated test for new behavior or error reproduce.
- Run affected tests by `./run test <test_id>`
2.	Green:
- Implement just enough code to make the test pass.
- Run the affected test(`./run test <test_id>`) to ensure everything is green.
3.	Refactor:
- Clean up code and tests without changing behavior.
- Eliminate duplication and improve readability.
- Run affected tests(`./run test <test_id>`) to confirm the correctness of the refactoring.
4.  Final check:
- Run all checks by `./run check` to finally ensure that everything is working fine. Fix all found issues, including linting errors and warnings.

## Tests rules

- Place tests in the tested packages and don't be afraid to use private methods for testing.
- Don't introduce code changes solely to pass tests without fixing underlying errors.
- Don't use code stubs; ensure all code is complete and functional.
- Trying to restart individual tests to speed up testing, but in the final check, run all tests

### Features for CommonUnitTests
- Using Nimble (and any third-party assertion libraries) is prohibited in the CommonUnitTests target.
- All checks must be performed only through standard XCTAssert/XCTFail/XCTAssertThrowsError, etc.
- Reason: Nimble is incompatible with the current Swift/Xcode configuration for Common (SPM framework), causing compilation and linking errors.
