# Текущая задача

**Завершено:** Исправление тестов LLMAPIClientTests по TDD

## Выполненные шаги:
- Запущены все проверки через ./run check, выявлены падающие тесты LLMAPIClientTests (401, 429, 500).
- Проанализированы тесты и реализация LLMAPIClient/LLMAPIClientError.
- Исправлены тесты: теперь они сравнивают errorDescription с ожидаемым описанием ошибки.
- Прогнаны только тесты LLMAPIClientTests — все прошли успешно.
- Прогнан полный ./run check — все проверки и тесты проходят.

**Задача завершена.**

# Задача: Сделать фон всех страниц приложения (MainApp + ShareExtension) белым

## Анализ
- Пользователь сообщил, что сейчас фон внутри контента серый, а должен быть белым.
- Проведён полный аудит кода MainApp и ShareExtension на предмет установки фона.
- В большинстве контейнеров и экранов уже используется Color.white, но в некоторых местах (особенно в формах и модальных окнах) фон мог перекрываться системным серым из-за особенностей SwiftUI.

## План
1. Проверить все основные контейнеры и экраны MainApp и ShareExtension на предмет явного задания белого фона.
2. Внести изменения:
   - Добавить .background(Color.white) для Form в AddOperationView и EditOperationView.
   - В BottomSheetContainerView (ShareExtension) сделать фон всегда белым и непрозрачным.
   - Исправить вызовы кастомного скругления углов, чтобы не было ошибок компиляции.
3. Запустить все проверки и тесты.
4. Если фон всё ещё не белый — искать причину глубже (глобальные стили, кастомные компоненты).

## Выполнено
- Внесены изменения в AddOperationView, EditOperationView, BottomSheetContainerView.
- Исправлены ошибки с RoundedCorner и UIRectCorner.
- Все проверки и тесты пройдены успешно (кроме известных падений в LLMAPIClientTests, не связанных с фоном).

## Следующий шаг
- Проверить визуально на симуляторе/устройстве, что фон действительно стал белым на всех страницах.
- Если потребуется — доработать стили для других компонентов.
