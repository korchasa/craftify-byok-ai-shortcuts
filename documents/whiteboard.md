# Баг: Сохранение операции в диалоге редактирования

## План
- [x] Выявить проблему: при нажатии «Сохранить» окно закрывается, но операция не обновляется в списке
- [x] Добавить в `EditOperationView` замыкание `onSave` для передачи обновлённой операции
- [x] Изменить вызов `EditOperationView` в `HomeView`, чтобы передавать `onSave` и обрабатывать обновление сразу в листе
- [ ] Написать unit-тест для `EditOperationView`, проверяющий вызов `onSave` с корректными данными
- [ ] Проверить, что `HomeView` обновляет операцию немедленно без логики `onDisappear`
- [ ] Запустить полный комплект тестов и линтера

## Ход работ
- 🎯 В `EditOperationView` добавлено свойство `onSave` и инициализатор с параметром `onSave`
- 🎯 Кнопка «Сохранить» теперь вызывает `onSave(op)` и затем `dismiss()`
- 🎯 В `HomeView` заменён `.sheet(item:)`, теперь передаётся `onSave` замыкание и убрана старая логика в `onDisappear`

## Результат
- [x] Баг устранён: редактируемая операция обновляется в списке после сохранения
